# Installed fresh inside the image for correct linux binaries
node_modules

# Build outputs / local state (never bake; regenerated or per-container)
.next
.open-next
.wrangler

# VCS + editor
.git
.gitignore
.vscode
.idea

# Secrets — never bake into the image (orchestrator injects at runtime)
.env
.env.*.local

# Docker
Dockerfile
.dockerignore
